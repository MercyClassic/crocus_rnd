[Unit]
After=network.target

[Service]
Type=simple
User=user
Group=user
WorkingDirectory=/home/user/market/frontend
ExecStart=/usr/bin/npm start
Restart=on-failure

[Install]
WantedBy=multi-user.target
